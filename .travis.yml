sudo: required
services: 
  - docker

before_install:
  - docker build -t khoa-le/react-test -f ./client/Dockerfile.dev ./client

script: 
  - docker run -t khoa-le/react-test npm test -- --coverage

after_success:
  - docker build -t khoa-le/multi-client ./client
  - docker build -t khoa-le/multi-nginx ./nginx
  - docker build -t khoa-le/multi-server ./server
  - docker build -t khoa-le/multi-woker ./worker

  #Login to docker hub
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #Take those images and push them to docker hub
  - docker push khoa-le/multi-client
  - docker push khoa-le/multi-nginx
  - docker push khoa-le/multi-server
  - docker push khoa-le/multi-worker

